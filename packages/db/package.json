{
	"name": "@my-better-t-app/db",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./src/index.ts"
		},
		"./*": {
			"types": "./dist/*.d.ts",
			"default": "./src/*.ts"
		}
	},
	"scripts": {
		"build": "tsdown",
		"db:push": "drizzle-kit push --dialect=postgresql --schema=./src/schema --url=postgresql://postgres:postgres@127.0.0.1:54322/postgres",
		"db:deploy": "drizzle-kit push",
		"db:studio": "drizzle-kit studio",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate",
		"db:reset": "supabase db reset"
	},
	"devDependencies": {
		"drizzle-kit": "^0.31.2",
		"@types/pg": "^8.11.11",
		"tsdown": "catalog:"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"drizzle-orm": "catalog:",
		"pg": "^8.14.1",
		"dotenv": "catalog:",
		"zod": "catalog:"
	}
}
